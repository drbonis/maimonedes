"""Command-line entry point.

Phase 0 placeholder: real subcommands land in later issues. The function
exists now so the `maimonedes` console script declared in pyproject.toml
resolves on a clean install.
"""
from __future__ import annotations


def main() -> None:
    print("maimonedes CLI placeholder. See `maimonedes --help` once subcommands land.")
